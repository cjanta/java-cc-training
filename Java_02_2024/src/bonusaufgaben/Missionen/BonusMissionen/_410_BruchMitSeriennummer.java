package bonusaufgaben.Missionen.BonusMissionen;

public class _410_BruchMitSeriennummer {

    /*
     * Bruch mit Seriennummer
     *
     * Leite von der Klasse Bruch aus der vorherigen Aufgabe die Subklasse BruchMitSeriennummer ab.
     * Ergänze die Klasse um ein ganzzahliges Attribut mit dem Namen seriennummer.
     * In dem Attribut soll jeder Bruch seine Seriennummer speichern.
     * Der zuerst erzeugte Bruch soll die Seriennummer 1 erhalten,
     * der zweite Bruch die Seriennummer 2 usw.
     * Für jeden weiteren Bruch wird die Seriennummer also um 1 erhöht.
     *
     * Hinweis:
     * Verwende zusätzlich ein statisches Attribut,
     * um die jeweils korrekte Seriennummer zu ermitteln.
     * Die Seriennummer eines Objekts soll
     * als unveränderliches (final-) Attribut definiert werden.
     * Für die Abfrage der Seriennummer soll die Klasse
     * eine Getter-Methode zur Verfügung stellen.
     *
     * Erstelle ein Programm mit dem Namen Seriennummerntest,
     * das fünf Brüche definiert und diese anschließend
     * mit ihren Seriennummern in der Konsole ausgibt.
     */

}
